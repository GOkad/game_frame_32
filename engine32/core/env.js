const env = {
    version: "v0.0.1",
    
    /**
     * Location refresh interval
     * !- DEBUG ONLY -!
     * false - off
     * int (ms)
     */
    dev_refresh: false,
    
    /**
     * Send event request
     * polling interval
     */
    poll_interval: 500,

    /**
     * Symbols map
     *   
     */
    symbols: {
        nl: "\n"
    }
};